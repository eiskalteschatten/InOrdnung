import React from 'react';

import styles from './styles.module.scss';

const SidebarSpacer: React.FC = () => {
  return (
    <div className={styles.spacer} />
  );
};

export default SidebarSpacer;
