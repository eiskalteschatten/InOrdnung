import React from 'react';

// import styles from './ProjectInfo.module.scss';

const ProjectInfo: React.FC = () => {
  return (
    <div>
      This is the ProjectInfo view
    </div>
  );
};

export default ProjectInfo;
